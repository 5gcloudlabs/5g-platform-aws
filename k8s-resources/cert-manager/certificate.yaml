apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-cert
  namespace: istio-system
spec:
  issuerRef:
    name: cluster-issuer
    kind: ClusterIssuer
  secretName: tls-cert-secret
  commonName: DOMAIN_NAME
  dnsNames:
    - DOMAIN_NAME
    - *.DOMAIN_NAME
