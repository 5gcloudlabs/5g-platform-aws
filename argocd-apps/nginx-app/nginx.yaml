# nginx-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: default
  labels:
    domain: ${ARGOCD_ENV_DOMAIN_NAME}
spec:
  containers:
    - name: nginx
      image: nginx:latest
      imagePullPolicy: Always
