#
# Software Name : towards5gs-helm
# SPDX-FileCopyrightText: Copyright (c) 2021 Orange
# SPDX-License-Identifier: Apache-2.0
#
# This software is distributed under the Apache License 2.0,
# the text of which is available at https://github.com/Orange-OpenSource/towards5gs-helm/blob/main/LICENSE
# or see the "LICENSE" file for more details.
#
# Author: Abderaouf KHICHANE, Ilhem FAJJARI, Ayoub BOUSSELMI
# Software description: An open-source project providing Helm charts to deploy 5G components (Core + RAN) on top of Kubernetes
#
# -------------------------------------------------------------------------
# [MODIFIED by 5G Cloud Labs]
# This file is customized for 5g-platform-aws.
# Networking parameters are inherited from the customized global values.yaml.
# -------------------------------------------------------------------------
#
# [MODIFIED by 5G Cloud Labs]
# Renamed nested parameter group: 'n2network' > 'n2networkgNB'
{{- if .Values.global.n2networkgNB.enabled }}
apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
# [MODIFIED by 5G Cloud Labs]
# Updated value for NAD name.
  name: {{ .Values.global.n2networkgNB.name }}
spec:
# [MODIFIED by 5G Cloud Labs]
# Replaced MACVLAN with IPVLAN configuration.
# Added IPAM configuration using Whereabouts.
  config: '{
      "cniVersion": "0.3.1",
      "plugins": [
        {
          "type": "{{ .Values.global.n2networkgNB.type }}",
          "capabilities": { "ips": true },
          "master": "{{ .Values.global.n2networkgNB.masterIf }}",
          "mode": "l2",
          "mtu": 9001,
          "ipam": {
            "type": "whereabouts",
            "range": "{{ $.Values.global.n2networkgNB.subnetIP }}/{{ $.Values.global.n2networkgNB.cidr }}",
            "range_start": "{{ $.Values.global.n2networkgNB.n2if.ipAddress }}",
            "range_end": "{{ $.Values.global.n2networkgNB.n2if.ipAddress }}",
            "routes": [{"dst": "{{ $.Values.global.n2networkgNB.routeDST }}",
            "gw": "{{ $.Values.global.n2networkgNB.gatewayIP }}"}]
         }
        }
      ]
    }'
{{- end }}
