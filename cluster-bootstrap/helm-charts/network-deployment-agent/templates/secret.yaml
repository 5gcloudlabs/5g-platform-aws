{{- /*
  No application secret is required when the platform repository is public.
  Manifests are fetched unauthenticated via GITHUB_RAW_BASE.
  Optional GITHUB_TOKEN can still be injected manually for private platforms.
*/ -}}
